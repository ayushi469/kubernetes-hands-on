apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-service
spec:
  type: ClusterIP
  selector:
    app: ingress-nginx-deployment
  ports:
  - port: 9090          ## this is service port
    targetPort: 9090    ## this is container port